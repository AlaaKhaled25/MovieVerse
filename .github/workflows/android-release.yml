name: Build & Release Android APK

on:


  push:
    tags:
      - "v*"

  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-android:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          flutter-version: 3.44.9
          cache: true

      - name: Restore dependencies
        run: flutter pub get

      - name: Generate TMDB API config
        run: |
          mkdir -p lib/config
          cat > lib/config/api_config.dart <<'EOF'
          class ApiConfig {
            ApiConfig._();
            static const String tmdbApiKey = '${{ secrets.TMDB_API_KEY }}';
          }
          EOF

      - name: Resolve release tag
        id: tag
        run: |
          if [[ "$GITHUB_REF_NAME" =~ ^v[0-9] ]]; then
            echo "tag=$GITHUB_REF_NAME" >> "$GITHUB_OUTPUT"
          else
            echo "tag=snapshot-$GITHUB_RUN_ID" >> "$GITHUB_OUTPUT"
          fi

      - name: Build release APK
        run: flutter build apk --release

      - name: Create/update GitHub Release with APK
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.tag.outputs.tag }}
          name: MovieVerse ${{ steps.tag.outputs.tag }}
          draft: false
          prerelease: false
          files: build/app/outputs/flutter-apk/app-release.apk
